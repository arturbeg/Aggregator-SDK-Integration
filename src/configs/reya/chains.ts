import type { Token } from '../../common/tokens'

export const REYA_COLLATERAL_TOKEN = {
  symbol: 'rUSD',
  name: 'Reya USD',
  decimals: 6,
  address: {
    42161: undefined,
    10: undefined
  }
} as Token
